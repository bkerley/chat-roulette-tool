!!!5
%html
  %head
    %title chat roulette
  %body
    %h1 pair up
    - 10.times do
      %ol
        - pairs.shuffle.each_slice(2).each do |p|
          %li
            - if p.length > 1
              = p.join ' & '
            - else
              = p.first
              (just join channel 1)
